好的，现在咱们继续开发main主分支，agent-mode分支作为备份。
现在我们需要实现这么一个手机端长时间的操作，不需要使用agent决策如何操作手机，而是通过一个脚本决定如何操作手机。脚本需要支持循环。脚本大致内容如下：
1、APP需要开启无障碍服务;
2、需要支持多个任务，每一个任务对应一个脚本。多任务不可并行执行，仅可选择一个任务，完成后才可再继续选择任务；
3、本次版本号升级到0.2.XXX版本，后续版本可能需要支持从服务器获取任务，现阶段调试，需要把脚本打包到apk，以简化V0.2.1版本的开发。
4、脚本可以使用文件文件，需要设计一下脚本的格式；
5、脚本执行的内容：
（1）首先打开一个脚本指定的APP，然后循环1000个轮次，或者执行一定时长；
（2）每个轮次内，包含1个或多个步骤；
（3）轮次首先需要读取屏幕内容；
（4）需要写判断当前屏幕所在APP名称是哪个；
（5）需要根据屏幕内容，在脚本中定义，满足什么条件时，定位需要操作的控件，操作的类型包括点击、长按、拖动；
（6）点击类操作需要定义参数：点击位置（或控件），随机偏移量，点击速度，以这些参数尽量接近人的操作；
（7）长按类操作需要定义参数：长按位置（或控件），随机偏移量，长按时长，
（8）拖动类操作需要定义：起始位置，起始随机偏移量，结束位置，过程随机偏移抖动，结束位置偏移，拖动速度；
（9）操作完成后完成一个轮次，开始计时，间隔随机时间3——5秒后，开始下一轮。
6、需要先设计一下脚本的格式，然后再开始开发。因为我了不太了解怎样设计这一类自动化操作脚本的格式。需要你的协助。我初步设想的方案使用汉字描述如下 ：
==打开“惊喜红包”APP
==读取屏幕内容，当前是否在目标APP惊喜红包APP内；
==如果不在目标APP，切换或打开“惊喜红包”APP，然后返回到上一步；
==如果在目标APP内，判断当前屏幕的内容是否有控件文字包括：邀请有奖，并且是否有控件文字包括：看视频，领金币。
    如果不满足条件，则进行下一步判断；
    如果满足条件，则定位需要点击的控件，定位条件是控件上的文字，或者纯图标按钮。满足条件的情况下，需要点击控件。
    按定义的操作点击后，本轮结束。
==上一条判断不满足，则进入下一条判断：判断当前屏幕内容是否有控件包含：“看完视频”并且是否有控件文字包含“或完成视频要求”
    如果不满足条件，则进行下一步判断；
    如果满足条件，则定义怎样操作。
==经过几个判断后都不满足条件，定义兜底的操作。比如没有找到满足的条件，那么关闭当前应用手，打开惊喜红包APP。或者也可以不满足条件的情况下，结束本轮，间隔时间后，开启下一轮操作。
这些是我设想的脚本的内容，请完善需要的内容并设计脚本格式。在后续工作中需要逐步根据具体任务，配置脚本的内容。
但是我不清楚自动化任务的时候，识别控件，定位控件和操作应该怎么在脚本中定义。请帮我设计一下。


